<%= form_for(@adoption_agreement) do |f| %>
  <% if @adoption_agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adoption_agreement.errors.count, "error") %> prohibited this adoption_agreement from being saved:</h2>

      <ul>
      <% @adoption_agreement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
